flask==3.1.0
requests==2.32.3
python-dotenv==1.0.1

# Production WSGI server. Flask's built-in server must not face a network —
# run `waitress-serve --listen=127.0.0.1:3000 server:app` behind nginx.
# waitress works on both Linux and Windows; on Linux gunicorn is fine too.
waitress>=3.0,<4
